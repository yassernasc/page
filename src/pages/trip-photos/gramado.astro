---
import Album from '@layouts/album.astro'

const frontmatter = {
  page_title: 'Gramado 🇧🇷',
  description: 'photos of my trip to gramado, brazil',
}

const photos = await Astro.glob('./gramado/*.webp')
---

<Album frontmatter={frontmatter} photos={photos} />
