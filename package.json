{
  "name": "yasser-page",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "predev": "npm run prepare-files",
    "prebuild": "npm run prepare-env && npm run prepare-files",
    "prepare-env": "scripts/prepare-env.sh",
    "prepare-files": "scripts/org-to-md.sh",
    "format": "npx prettier --write ."
  },
  "dependencies": {
    "astro": "^2.1.7",
    "astro-seo": "^0.7.4",
    "ramda": "^0.29.0"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-astro": "^0.8.0"
  }
}
